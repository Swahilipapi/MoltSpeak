{
  "name": "@moltspeak/registry",
  "version": "0.1.0",
  "type": "module",
  "description": "MoltSpeak Agent Registry - Discovery service for the agent internet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "REGISTRY_URL=https://registry.moltspeak.xyz vitest run"
  },
  "dependencies": {
    "@libsql/client": "^0.5.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
